[package]
name = "easy_wal"
version = "0.1.0"
edition = "2024"

[features]
default = []
stats = []
compression = ["snap", "zstd"]

[dependencies]
thiserror = "*"
crc32fast = "*"
memmap2 = "0.9"
snap = { version = "*", optional = true }
zstd = { version = "*", optional = true }

[dev-dependencies]
tempfile = "*"
serial_test = "*"
